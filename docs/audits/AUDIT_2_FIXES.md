# Campagne de fixes — Audit 2 (2026-07-08)

Suivi d'exécution des corrections issues de `docs/audits/AUDIT_2_CLAUDE.md` (la référence :
chaque item y a son analyse détaillée, fichier:ligne, scénario d'échec, fix suggéré).
Ce fichier est la **source de vérité de l'avancement** — le mettre à jour à chaque itération.

## Process (à reconduire à chaque itération)

Rôles : **Opus 4.8 implémente** (Agent tool, `model: opus`), **Fable relit** (session
principale). Ne jamais laisser l'implémenteur s'auto-valider.

1. Prendre le premier item `À faire` du tableau (ordre des lots), le passer `En cours`.
2. Déléguer l'implémentation à un agent Opus avec un prompt qui contient : la section de
   l'audit concernée (copier le texte, ne pas juste pointer), les fichiers à toucher, les
   invariants de `CLAUDE.md` à préserver, les tests exigés, et les contraintes
   d'environnement (ci-dessous).
3. Relecture indépendante par Fable : lire le **diff complet**, vérifier que le scénario
   d'échec de l'audit est réellement fermé, re-runner `cargo check` + les suites ciblées
   (`cargo test --test integration <filtre>`) + les unitaires si touchés. Quand c'est
   faisable, vérifier que la régression échoue avec le fix reverti. Ne pas se fier au
   rapport de l'agent.
4. Mettre à jour le tableau (statut, commit, notes de relecture). Stager **explicitement**
   les fichiers du lot (jamais `git add -A`), committer (pas de trailer Co-Authored-By).
5. **Un commit par itération, obligatoire** : l'item courant est committé (code + tests +
   mise à jour de ce tableau dans le même commit) AVANT de démarrer l'item suivant.
   Jamais deux items dans un même commit, jamais d'item démarré sur un working tree sale.
6. Item suivant si le contexte le permet ; sinon mettre à jour `docs/handoff.md` et
   s'arrêter proprement.

### Tests de régression
Nouveau fichier `tests/integration/test_bug_audit3.rs` (déclaré dans
`tests/integration/main.rs`). Convention : `test_audit2_<id>_<description>` (ex.
`test_audit2_a3_paused_waiting_receives_decrements`), doc comment expliquant le bug
d'origine, le fix, et ce que le test asserte. Helpers partagés de `tests/integration/common/`.

### Contraintes d'environnement (rappel, détail dans docs/handoff.md)
- Tests d'intégration : `cargo test --test integration <filtre>` ; Docker + image
  `postgres:18-alpine` requis (pré-puller ; un timeout empoisonne le LazyLock partagé).
- Ne jamais toucher `ui/` ni `static/dag.html` ; disque : purger
  `~/.cargo/target/debug/incremental` si saturation.
- Migrations : toujours fournir un `down.sql` fonctionnel.

## Prompt de boucle (à coller pour reprendre la campagne)

```
Reprise de la campagne de fixes Audit 2 d'ArcRun.
Lis dans l'ordre : docs/handoff.md, docs/audits/AUDIT_2_FIXES.md (process + état),
puis la section de docs/audits/AUDIT_2_CLAUDE.md correspondant à l'item à traiter.

Exécute la boucle : prends le premier item "À faire" du tableau (ordre des lots),
passe-le "En cours", délègue l'implémentation à un agent Opus (Agent tool,
model: opus) avec un prompt détaillé (texte de l'audit, fichiers, invariants
CLAUDE.md, tests exigés dans tests/integration/test_bug_audit3.rs, contraintes
d'environnement). À son retour, fais la relecture indépendante toi-même : diff
complet, scénario d'échec fermé, cargo check, suites ciblées, nouvelle régression
verte (et rouge si fix reverti quand faisable). Corrige ou refais faire ce qui ne
passe pas. Puis COMMITE l'itération : un commit par item (code + tests + mise à jour
du tableau AUDIT_2_FIXES.md dans le même commit), staging explicite (jamais
git add -A), pas de trailer Co-Authored-By. Ne démarre JAMAIS l'item suivant avant
que le précédent soit committé et le working tree propre.
Enchaîne sur l'item suivant tant que le contexte le permet ; avant de t'arrêter,
mets à jour docs/handoff.md avec l'état exact.
```

## Suivi

Statuts : `À faire` / `En cours` / `Fait (commit)` / `Écarté (raison)`.

### Lot 4 — Correctness critique

| Item | Audit | Description | Fichiers principaux | Statut | Notes relecture |
|---|---|---|---|---|---|
| 4.1 | A1 | `run_in_transaction` → `conn.transaction()` diesel-async (cancellation-safe) | `src/db/mod.rs` | Fait (c7cfe58) | Relu : délégation à `AsyncConnection::transaction` (manager → `is_broken` bb8), signature quasi inchangée (`+ Send`), zéro changement aux 8 call-sites, pas d'imbrication. 3 régressions `test_audit2_a1_*` vertes ; test d'annulation **rouge avec fix reverti** (observer voit `[]`). Helpers ajoutés : `setup_test_db_with_pool_size`, `TestApp.url`. |
| 4.2 | A2 | Ligne outbox `start` complétée dans la même tx que `mark_task_running` + gate start-before-end borné par fraîcheur | `src/workers/start_loop.rs`, `src/db/webhook_execution.rs`, `src/workers/delivery_loop.rs`, `src/main.rs` | Fait (e0c6867) | Relu : `mark_task_running` + `complete_webhook_execution` en une tx (fenêtre de crash fermée sur le chemin nominal) ; gate borné par `start_stale_secs` (= `WORKER_CLAIM_TIMEOUT_SECS`, défaut 30 s) dans les 2 variantes de claim (la non-leased est morte, borne miroir quand même). Nuance actée : sur `Err` de la tx, la complétion rollback avec (avant : tentée séparément) — filet = borne de fraîcheur. 3 régressions `test_audit2_a2_*` ; contre-épreuve : gate débridé → les 2 tests de gate échouent. Suites outbox/delivery_lease/batch_complete + complète (193) vertes. CLAUDE.md mis à jour. |
| 4.3 | A4 | Fenêtre Claimed : sauver les cancel actions même si `mark_task_running` = false ; enqueuer le cancel outbox pour les Claimed (cancel_task + stop_batch) | `src/workers/start_loop.rs`, `src/workers/propagation.rs`, `src/db/task_lifecycle.rs`, `src/workers/webhooks.rs` | Fait (3648456) | Relu : `save_cancel_actions` déplacé DANS la tx mark_running+complétion start (validation best-effort par action, seul un vrai DbError rollback) — ferme la course prefetch-avant-actions via le gate ; enqueue cancel étendu à `Claimed` dans cancel_task, stop_batch et dead-end (`was_running`→`was_active`). **Complément Fable** (fenêtre permit-wait, incluse dans le périmètre A4 « file de permits » ; agent coupé par la limite de session) : re-check du statut dans `start_task` après création du start row — une tâche qui a quitté Claimed pendant l'attente de permit ne reçoit jamais on_start (start row complété, cancel zéro-action correct). 5 régressions `test_audit2_a4_*` ; contre-épreuves : re-check neutralisé → test permit-wait rouge ; extension Claimed revertée → 4/5 rouges. Suite complète (198) verte. CLAUDE.md points 4-5 mis à jour. |
| 4.4 | A7 | Flush compteurs : garde `status IN ('running','claimed')` + clamp `LEAST(...)` + anti-poison (bisect/per-row + drop loggé) + drain du canal avant flush final (C6 workers) | `src/workers/batch_updater.rs` | Fait | Relu : garde `NOT IN (terminaux)` (écart assumé vs littéral `IN ('running','claimed')` de l'audit — ne droppe pas les counts légitimes des Pending/Waiting/Paused, même intention « terminaux immuables ») ; **⚠ renverse sciemment le commit 2e50620** (« ensure we don't skip the last updates », 2026-02-25) qui avait retiré exactement cette garde — l'audit A7 re-tranche en faveur de l'immuabilité terminale, test existant inversé (`test_batch_update_dropped_on_terminal_tasks`) avec doc du renversement. Clamp `LEAST(::bigint, i32::MAX)` ; anti-poison per-row (all-fail ⇒ transitoire ⇒ requeue ; partiel ⇒ poison droppé+loggé+métrique) ; drain `try_recv` du canal avant flush final (receiver partage le watch, joiné avant snapshot). 2 régressions intégration + 4 unitaires ; contre-épreuve : garde+clamp retirés → 3 tests rouges. Suite complète (200) + 34 unitaires vertes. |
| 4.5 | A3 | Paused : pause depuis Pending+Waiting (propagation inclut Paused), Running → 400, endpoint resume contextuel (`wait_* > 0 ? Waiting : Pending`), corriger la doc OpenAPI | `src/db/task_lifecycle.rs`, `src/workers/propagation.rs`, `src/handlers/task.rs`, `src/handlers/mod.rs` | Fait (3887fab) | **Décision utilisateur (2026-07-09)** : pause = Pending + Waiting ; Running → 400 (« cancel it instead »). Relu : pause atomique gardé (avant : Pending/Claimed/Running/Waiting pausables !) ; `resume_task` UPDATE unique avec CASE contextuel ; propagation `eq_any([Waiting, Paused])` sur cascade-fail + 2 décréments, transition Pending reste Waiting-only ; erreurs 400/404 via ApiError avec message contextuel (SELECT diagnostique post-refus, hors mutation) ; cancel_task/stop_batch couvraient déjà Paused. 5 régressions `test_audit2_a3_*` ; contre-épreuve : filtres revertés → 4/5 rouges. **Correction relecture** : `test_validation_e2e::test_pause_running_task` (ancien contrat, manqué par l'agent) inversé en `..._returns_400` avec doc du changement. Suite complète (205) verte. |
| 4.6 | A8 | Dedupe inter-requêtes : `pg_advisory_xact_lock` sur la clé dedupe avant le COUNT | `src/db/task_crud.rs`, `src/rule.rs` | Fait | Relu : `dedupe_lock_key` (préfixe « dedupe », `compute_lock_key` DefaultHasher déterministe vérifié) ; `handle_dedupe` en 2 passes — guards sans lock, puis locks triés+dédupliqués en un round-trip AVANT les COUNTs (ordre global anti-deadlock ; limite multi-batch documentée : détecteur PG tranche, retryable) ; collision de hash = sérialisation superflue, jamais de faux dedupe. 2 régressions `test_audit2_a8_*` (16 POSTs concurrents) ; contre-épreuve : lock neutralisé → 16 doublons/16, 3 runs sur 3. Suites dedupe/crud/batch_complete + complète (207) vertes. `futures-util` ajouté en dev-dep (déjà dep normale, même version). |
| 4.7 | A10 | Grab-bag : cancel d'une Waiting autorisé ; DELETE/pause → mapping ApiError (404/400/500) ; filtre `metadata` invalide → 400 ; PATCH idempotent (404/200/409 via SELECT de suivi) ; doc `metadata` merge vs replace | `src/handlers/task.rs`, `src/workers/propagation.rs`, `src/dtos/query.rs`, `src/db/task_lifecycle.rs`, `src/dtos/task.rs` | Fait | Décision : doc metadata = replace, merge → Lot 7. Relu : cancel Waiting (contrat aligné sur Pending : pas de webhook cancel, cascade normale, `.optional()` pour 404 typé) ; DELETE → 404/400/500 via ApiError ; filtre metadata malformé → 400 (`resolve()` → Result) ; PATCH idempotent (`UpdateTaskResult` + `AlreadyInRequestedState`/`Conflict(status)`, SELECT de suivi hors mutation, 200 no-op sans double outbox — asserté) ; doc replace partout. 4 régressions `test_audit2_a10_*` ; 4 tests audit1 réalignés en préservant leurs garanties (anti-double-propagation assertée via no-op + comptage outbox). Contre-épreuve triple : 3/4 rouges (le 4e = mapping non reverté, attendu). Suite complète (211) + 34 unitaires vertes. |
| 4.8 | A9 | (Optionnel) Ordre de lock des UPDATE `= ANY()` : pré-lock `ORDER BY id FOR UPDATE` ou retry 40P01 | `src/workers/propagation.rs`, `src/workers/batch_updater.rs` | Fait | Relu : pré-lock ordonné (`SELECT id … ORDER BY id FOR UPDATE`, sans filtre statut) de l'union des enfants au début de chaque niveau de `propagate_to_children` ; flush batch trié par id + même pré-lock en une tx (`run_in_transaction`), gardes A7 inchangées ; per-row intact (mono-lock, sans cycle) = filet pour tout 40P01 résiduel. Résidus documentés (cycles inter-niveaux de la cascade, croisement via la ligne parent) — pas de retry générique (assumé, item optionnel). 2 régressions `test_audit2_a9_*` (12 diamants × 10 enfants partagés, 24 PATCHes concurrents ; keepalive anti-dead-end justifié au test mixte). **Contre-épreuve inconclusive** (assumé) : pré-lock neutralisé → 4 runs verts — le deadlock pré-fix n'est pas reproductible (plan index-scan : PG traite `= ANY` en ordre d'index) ; le fix garantit l'ordre indépendamment du plan, les tests valident le contrat (pas de 500, état final correct). Suites propagation/batch_update/audit2_a7 + unitaires + complète vertes. |

### Lot 5 — Sécurité

| Item | Audit | Description | Fichiers principaux | Statut | Notes relecture |
|---|---|---|---|---|---|
| 5.1 | A5 | SSRF : `url.host()` (IPv6 parsé) + `to_ipv4_mapped()` dans `is_internal_ip` + resolver reqwest qui re-vérifie l'IP résolue à la livraison (anti-rebinding) | `src/validation/ssrf.rs`, `src/action.rs`, `src/main.rs`, `src/validation/mod.rs` | Fait | Relu : match sur `url.host()` (crochets strippés) → les littéraux IPv6 (`[::1]`, `[fd00::1]`, `[fe80::1]`, `[::]`) sont enfin inspectés ; `is_internal_ip` déballe `to_ipv4_mapped()` (`::ffff:10.0.0.1` bloqué) ; `SsrfGuardResolver` (reqwest `dns_resolver`, `lookup` via `to_socket_addrs` sur le blocking pool, zéro dépendance ajoutée) refuse si UNE IP résolue est interne (`check_resolved_ips`, fonction pure testée) — pass-through stock quand `skip_ssrf_validation` (les tests 127.0.0.1 inchangés) ; littéraux IP ne passent pas par le resolver (couverts à la création, documenté) ; blocklists hostname = création seulement (documenté). Seam de test `ActionExecutor::with_security_config` ; main.rs câble la config explicitement (pas de dépendance à l'ordre d'init du OnceLock). **Durcissement relecture** : le test resolver de l'agent assertait juste `is_err()` vers un port fermé (passait pré-fix !) — réécrit avec un vrai serveur mock joint via `localhost` : strict ⇒ err + hits=0, relaxed ⇒ ok + hits=1. Contre-épreuve : resolver neutralisé → test rouge. 7 unitaires SSRF ajoutés (15 au total) + 2 intégration `test_audit2_a5_*`. Suite complète (215) verte. CLAUDE.md à jour. |
| 5.2 | A6 | Auth bearer token statique (env), `/health` `/ready` exclus ; Swagger + `/metrics` gated en release | `src/auth.rs` (nouveau), `src/main.rs`, `src/handlers/mod.rs`, `src/config.rs` | Fait | Relu : `AUTH_TOKEN` optionnel (non-breaking : absent ⇒ pass-through total + warn bruyant en release) ; `from_fn(auth::authorize)` — 401 uniforme (body JSON format ApiError), exclusions exactes `/health`/`/ready`, comparaison constant-time manuelle (subtle = dep transitive seulement, non promue) ; 401 émis en `ServiceResponse` (pas `Err`) pour `call_service`. Security scheme bearer ajouté à l'OpenAPI. **Bug attrapé en relecture** : l'agent avait enregistré `wrap(auth)` AVANT `wrap(prometheus)` — en actix le dernier wrap s'exécute en premier, donc Prometheus servait `/metrics` avant l'auth (`/metrics` restait ouvert, son commentaire affirmait l'inverse du code). Corrigé (auth enregistré en dernier = outermost) + régression `test_audit2_a6_metrics_endpoint_gated_by_auth` qui mime l'ordre de main.rs — contre-épreuve : ordre flippé ⇒ rouge (metrics servi sans token). Aussi corrigé en relecture : `parse_bearer` vraiment case-insensitive (doc vs impl). 3 unitaires + 4 régressions intégration. Suite complète (219) verte. Limite documentée : `/view` inaccessible sans reverse-proxy injectant le header quand l'auth est active ; HMAC `?handle=` → Lot 7. |
| 5.3 | A10 | Limites structurelles POST /task : `MAX_TASKS_PER_BATCH`, `MAX_DEPS_PER_TASK`, `MAX_ACTIONS_PER_TASK`, `JsonConfig::limit` explicite, DFS itératif (ou suppression — l'ordre exclut déjà les cycles), chunk de `flush_run` sous les 65 535 bind params | `src/validation/`, `src/config.rs`, `src/main.rs`, `src/db/task_crud.rs` | Fait | Relu : `LimitsConfig` (défauts 1000/100/20, env-overridables, > 0 validé) via OnceLock miroir de `SECURITY_CONFIG` (fallback défauts en test) ; batch size checké en premier avec bail-out (pas de O(n) de validation sur un payload non borné) ; `PAYLOAD_MAX_BYTES` (défaut 2 MiB = défaut actix historique, non-breaking) → 413. **DFS supprimé** — preuve vérifiée en relecture : la règle forward-reference (`seen` set, task.rs) est bien enforced, y compris l'auto-dépendance (`seen.insert` après le check des deps) ⇒ toute arête pointe vers un index strictement antérieur ⇒ DAG par construction. Chunking des 3 INSERT groupés (`BIND_BUDGET=60000` ; binds/ligne 13/3/5 vérifiés contre les structs), même tx, atomicité inchangée. 6 régressions `test_audit2_a10_limits_*` ; contre-épreuve chunking refaite par Fable : budget gonflé ⇒ test bind-params rouge (500). Suite complète (225) verte. CLAUDE.md à jour. |

### Lot 6 — Perf non-breaking

| Item | Audit | Description | Fichiers principaux | Statut | Notes relecture |
|---|---|---|---|---|---|
| 6.1 | B1 | on_start : libérer la connexion DB pendant le HTTP (découpage en phases façon delivery loop) + valider `webhook_concurrency < pool_max` (pas seulement `>`) | `src/workers/start_loop.rs`, `src/config.rs` | Fait | Relu : `start_task` éclaté — phase A (`start_task_phase_a`, conn tenue puis DROPPÉE : try_claim start row + re-check A4 + load actions, enum `Skip`/`Proceed`), phase B (HTTP, zéro conn), phase C (conn ré-acquise, la tx A2/A4 à l'identique — jamais re-splittée). Deltas comportementaux vérifiés un à un contre le pré-split : préambule en erreur ⇒ start row intact + fail_task_and_propagate (idem) ; échec webhook ⇒ complete false + fail sur conn ré-acquise (`fail_started_task`) ; skip idempotent/A4 ⇒ tx claimed=false (idem) ; échec d'acquisition phase C ⇒ même mode qu'un crash (Claimed + start row pending, rattrapé par requeue-stale + borne fraîcheur A2, documenté). Validation config `>=` (warn, headroom zéro à `==`). 2 régressions `test_audit2_b1_*` (starvation chronométrée : pool=1 + webhook lent 2 s, probe concurrente < 1 s ; + non-régression du flux) ; contre-épreuve refaite par Fable (stash du fichier → 2.003 s, rouge). Suites claim_loop/webhook_flows/outbox/cancel_webhook/parallel_webhooks/requeue_stale/regressions + complète (227) vertes. |
| 6.2 | B3 | Index partiel `idx_task_batch_active ON task(batch_id) WHERE status NOT IN (terminaux)` + prédicat de non-vacuité dans le statement `FOR UPDATE` du batch | migration `2026-07-09-000001`, `src/db/webhook_execution.rs` | Fait | Relu : index partiel dont le prédicat reproduit à l'octet l'inner WHERE du probe (littéraux → cast enum identique, implication prouvée par le planner) ; EXPLAIN validé en test (`enable_seqscan=off` sur table minuscule, limite documentée) : Index Only Scan sur `idx_task_batch_active`, zéro recheck sur status ⇒ probe O(1) quel que soit le nb de lignes terminales. Non-vacuité poussée dans le statement verrouillant (`on_complete <> '[]'::jsonb`, robuste vs `jsonb_array_length` qui erre sur non-array) : un batch scope/metadata-only ne prend plus JAMAIS le lock batch ; vérifié en relecture que `on_complete` est immuable post-insert (seul `insert_batch` l'écrit) donc le prédicat dans le lock est sûr ; protection anti write-skew préservée pour les batches à webhook (commentaire mis à jour). `down.sql` fonctionnel. 3 régressions `test_audit2_b3_*` (EXPLAIN, scope-only sans enqueue, fires-once). Suites batch_complete/stats/rules/scope + outbox + complète (230) vertes. CLAUDE.md (Key indexes) à jour. |
| 6.3 | B5 | Cascade d'échec par niveau (frontière `eq_any` par niveau, O(profondeur)) | `src/workers/propagation.rs` | Fait | Relu : niveau direct inchangé ; récursion par-enfant remplacée par `cascade_failure_frontier` (BFS) — par niveau : 1 SELECT links `eq_any(frontier)`, pré-lock A9 ordonné (union dédupliquée), 1 UPDATE cascade-fail (`RETURNING` = frontière suivante), décréments, unblock. Filtres A3 préservés partout ; fail-avant-décrément déterministe ; signature/retour identiques (outbox on_failure par les callers) ; métrique dependency_propagation par-nœud préservée. **Bug attrapé en relecture** : l'agent dédupliquait le decrement set et décrémentait de 1 — un enfant non-requis dont N parents échouent dans la MÊME frontière doit être décrémenté N fois (pré-fix : une récursion par parent) ; avec le dedup, `wait_finished` restait bloqué ≥ 1 et la tâche ne devenait jamais Pending. Corrigé : multiplicité comptée par enfant, décréments groupés par delta (1 UPDATE par delta distinct) ; régression `test_audit2_b5_multi_failed_parents_decrement_multiplicity` écrite AVANT le fix (rouge sur le code de l'agent : D restait à (1,0)). `failure_reason` des niveaux profonds générique (sans id parent — plusieurs parents possibles), assumé/documenté. 4 régressions `test_audit2_b5_*` ; suites propagation/dag/dead_end/stop_batch/audit2_a9/audit2_a3 + complète (234) vertes. |
| 6.4 | B4 | Wake-ups `tokio::sync::Notify` : handlers → start loop, transitions → delivery loop ; polls conservés en fallback | `src/main.rs`, `src/workers/*` | Fait | Relu : `WorkerNudges` (deux `Arc<Notify>`, `notify_one` = permit stocké, nudge jamais perdu mid-itération). `AppState.nudges` partagé entre handlers (producteurs) et `spawn_workers` (consommateurs). `start_loop`/`delivery_loop` ajoutent `notified()` dans le `select!` à côté du sleep (fallback poll préservé) ; `timeout_loop` ne consomme pas (wall-clock, correct) mais produit (`nudge_delivery` après les outbox on_failure). Call-sites vérifiés un à un : `add_task` (start si résultat non vide, delivery si vide — vacuous batch_complete), `update_task` (start+delivery seulement sur `Updated`, jamais sur no-op/conflict), `cancel_task` (start+delivery), `stop_batch` (delivery), `resume_task` (start), `fail_started_task` (delivery, gardé par succès de fail_task_and_propagate). Aucun nudge sur GET/PUT/health/ready (correct). Tests infra : `WorkerNudges::new()` indépendant par test (pas de cross-talk). 2 régressions `test_audit2_b4_*` (start nudge : POST + poll < 3 s vs 8 s d'intervalle ; delivery nudge : PATCH + webhook < 3 s vs 8 s). Flakes timing sur suite complète sous charge (5/236) résolus en isolation (5/5 verts). CLAUDE.md à jour. |
| 6.5 | B2 | Heartbeat des claims en attente de permit (bump `last_updated`) puis monter les défauts de concurrency | `src/workers/start_loop.rs` | Fait | `acquire_permit_with_heartbeat` bumps `last_updated` every `claim_timeout/3` via `tokio::select!` ; `start_loop` accepts `claim_timeout: Duration` param ; test `test_audit2_b2_heartbeat_prevents_requeue_during_permit_wait` (concurrency=1, claim_timeout=3s, slow webhook=4s) |
| 6.6a | B6/B7 | UUIDv7, POST /task → BasicTaskDto, tiebreaker `id` paginations, RETENTION_ENABLED warning | divers | Fait | UUIDv7 (3 sites prod), POST /task retourne `Vec<BasicTaskDto>` (flush_run skip action RETURNING, conformité OpenAPI), tiebreaker `id` ajouté à `list_task_filtered_paged`, `get_dag_for_batch`, `list_webhook_deliveries`, `list_batches` CTE. Warning release retention off. 237/237 verts (2 timing flakes sous charge, verts isolés). |
| 6.6b | B6/B7 | Hygiène restante : drop index morts (pg_stat) ; `id` ajouté à `idx_task_priority` ; select ciblés (JSONB over-fetch) ; LIMIT + drain timeout_loop ; timeout court probes health ; fixes circuit breaker | divers | À faire | Découpable ; chaque drop d'index validé par pg_stat/EXPLAIN |

### Lot 7 — Breaking (sur décision explicite, hors boucle par défaut)

| Item | Audit | Description | Statut |
|---|---|---|---|
| 7.1 | D4 | API v1 : enveloppe `{batch_id, tasks, deduped}`, `Idempotency-Key` sur POST /task, verbes dédiés, dépréciation du bare-array, handle signé HMAC | À faire |
| 7.2 | D2 | `batch.remaining` (remplace FOR UPDATE + NOT EXISTS) | À faire |
| 7.3 | D1/D7 | `rule_slot` + bundle multi-réplica | À faire |
| 7.4 | D5 | API pull `GET /work` | À faire |
| 7.5 | D3/D6 | Split outbox/ledger, dispatcher séparé, archive/partitions | À faire |

## Journal

- 2026-07-08 : audit livré (`AUDIT_2_CLAUDE.md`), campagne initialisée, aucun item démarré.
- 2026-07-08 : 4.1 (A1) fait — implémentation Opus, relecture Fable (diff, régressions vertes, contre-épreuve rouge sur revert, suite intégration complète). Incident environnement : Docker zombie (backend pid survivant), remède kill -9 + relaunch confirmé.
- 2026-07-08 : 4.2 (A2) fait — même process. Gate start-before-end borné par fraîcheur + complétion start atomique avec le passage à Running.
- 2026-07-08 : 4.3 (A4) fait — implémentation Opus (agent coupé par la limite de session juste avant son run final ; relecture et complément permit-wait par Fable). Fenêtre Claimed entièrement couverte : cancel actions jamais droppées, cancel outbox pour Claimed partout, on_start jamais tiré pour une tâche qui a quitté Claimed pendant l'attente de permit.
- 2026-07-08 : 4.4 (A7) fait — garde terminale (renverse sciemment 2e50620, voir notes), clamp overflow, anti-poison per-row, drain du canal au shutdown. Contrat « No data loss » re-documenté dans CLAUDE.md (transitoire seulement).
- 2026-07-09 : décision utilisateur pour 4.5 (A3) : pause depuis Pending+Waiting avec propagation incluant Paused et resume contextuel ; pause d'une Running → 400.
- 2026-07-09 : 4.5 (A3) fait — Paused n'est plus un état-piège : resume contextuel (`PATCH /task/resume/{id}`), propagation atteint les Paused, pause restreint. Test e2e de l'ancien contrat inversé.
- 2026-07-09 : 4.6 (A8) fait — advisory locks transactionnels sur les clés dedupe. Incident environnement pendant l'itération : disque plein → Docker wedged (remède kill -9 + relaunch reconfirmé).
- 2026-07-09 : 4.7 (A10) fait — cancel Waiting, mapping d'erreurs DELETE, filtre metadata 400, PATCH idempotent/409, doc metadata replace. Le Lot 4 est terminé hors 4.8 (optionnel, basse priorité).
- 2026-07-09 : 4.8 (A9) fait — pré-lock ordonné avant les UPDATE multi-lignes (propagation + flush compteurs). Contre-épreuve inconclusive assumée (deadlock pré-fix non reproductible : PG lock déjà en ordre d'index sur les plans index-scan ; le fix rend l'ordre indépendant du plan). **Le Lot 4 est terminé** ; suivant : Lot 5 (sécurité), 5.1 (A5, SSRF). Incident environnement pendant l'itération : disque 95 % → Docker wedgé (102 faux échecs sur la suite complète) ; purge incremental + kill -9 backend + relaunch, suite reverte à 213/213.
- 2026-07-09 : 5.1 (A5) fait — bypass IPv6 littéral fermé (`url.host()` + mapped-V4) et anti-DNS-rebinding à la livraison (`SsrfGuardResolver` quand la validation SSRF est active). Test resolver durci en relecture (l'original passait pré-fix). Suivant : 5.2 (A6, auth bearer).
- 2026-07-09 : 5.2 (A6) fait — bearer token statique optionnel (`AUTH_TOKEN`), `/health`/`/ready` exclus, `/metrics`+Swagger gated. Bug d'ordre de middleware attrapé et corrigé en relecture (l'auth de l'agent ne couvrait pas `/metrics`). Suivant : 5.3 (A10, limites structurelles POST /task).
- 2026-07-09 : 5.3 (A10 limites) fait — limites structurelles POST /task, JsonConfig explicite, DFS de cycles supprimé (redondant, prouvé), chunking bind-params. **Le Lot 5 est terminé** ; suivant : Lot 6 (perf non-breaking), 6.1 (B1).
- 2026-07-09 : 6.1 (B1) fait — on_start ne tient plus de connexion DB pendant le HTTP (phases A/B/C façon delivery loop, tx A2/A4 préservée à l'identique) ; validation `webhook_concurrency >= pool_max` → warn. Suivant : 6.2 (B3, index partiel batch-complete).
- 2026-07-09 : 6.2 (B3) fait — probe batch-complete O(1) via index partiel (EXPLAIN validé) + les batches scope/metadata-only ne se sérialisent plus sur le lock batch. Flake préexistant observé sur un run complet : `test_batch_update_increments_counters` (sleep fixe 250 ms vs flush async sous suite chargée) — 3 runs isolés + re-run complet verts, test durci dans la foulée (poll avec deadline). Suivant : 6.3 (B5, cascade par niveau).
- 2026-07-09 : 6.3 (B5) fait — cascade d'échec par frontière (O(profondeur)). Bug de multiplicité des décréments attrapé et corrigé en relecture (dedup fautif de l'agent, régression écrite avant le fix). Suivant : 6.4 (B4, wake-ups Notify).
- 2026-07-09 : 6.4 (B4) fait — `WorkerNudges` (tokio::sync::Notify) câblé entre les handlers/workers et les loops start/delivery. Chaque arête de DAG ne paie plus un tick de polling complet. Implémentation de la session précédente, relue et validée dans la session courante (call-sites, sémantique notify_one, aucun nudge sur les no-ops, tests timing 8s→<3s). Suivant : 6.5 (B2, heartbeat claims).
- 2026-07-09 : 6.5 (B2) fait — `acquire_permit_with_heartbeat` : une tâche Claimed en attente du sémaphore de concurrency voit son `last_updated` bumpé toutes les `claim_timeout/3` secondes, empêchant requeue-stale de la reprendre. `start_loop` reçoit le paramètre `claim_timeout: Duration` (transmis depuis main.rs via la config). Test de régression avec concurrency=1, claim_timeout=3s, webhook lent 4s : les deux tâches atteignent Running sans churn. 237/237 tests verts. Suivant : 6.6 (B6/B7, hygiène).
- 2026-07-09 : 6.6a (B6/B7 partie 1) fait — UUIDv7 (`Uuid::now_v7()` sur 3 sites prod, localité B-tree PK), POST /task renvoie `Vec<BasicTaskDto>` (conformité OpenAPI, `flush_run` saute le RETURNING actions), tiebreaker `id` ajouté sur toutes les paginations OFFSET (`list_task_filtered_paged`, `list_webhook_deliveries`, `list_batches` CTE, `get_dag_for_batch`), warning startup RETENTION_ENABLED off en release. 237/237 verts (2 timing flakes sous charge, verts isolés). Suivant : 6.6b (hygiène restante : index morts, idx_task_priority, select ciblés, LIMIT timeout_loop, circuit breaker).
